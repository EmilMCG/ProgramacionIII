# For Firebase, set a system property or env var to the service account JSON path
# Example (Docker): -Dfirebase.service.account=/run/secrets/firebase-service-account.json
# Or set environment variable: FIREBASE_SERVICE_ACCOUNT=/path/to/serviceAccount.json
